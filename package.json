{
  "type": "module",
  "scripts": {
    "build": "esbuild src/browser.js --bundle --minify --outfile=offer-importer.bundle.js",
    "test": "node test_importer.mjs"
  },
  "dependencies": {
    "exceljs": "^4.4.0",
    "xlsx": "^0.18.5"
  },
  "devDependencies": {
    "esbuild": "^0.25.5"
  }
}
